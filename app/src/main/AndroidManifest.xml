<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appeducacional">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



    <activity android:name=".Activities.MenuAlunoActivity"/>
        <activity android:name=".Activities.MenuProfessorActivity"/>
        <activity android:name=".Activities.MenuAdminActivity"/>
        <activity android:name=".Activities.CadastroActivity"/>
        <activity android:name=".Activities.InformacoesActivity"/>
        <activity android:name=".Activities.QuizActivity"/>
    </application>

</manifest>